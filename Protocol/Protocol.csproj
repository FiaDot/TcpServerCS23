<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net7.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <CustomCommands>
      <CustomCommands>
        <Command>
          <type>BeforeBuild</type>
          <command>build-apple-silicon.sh</command>
          <workingdir></workingdir>
        </Command>
      </CustomCommands>
    </CustomCommands>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' " />
  <ItemGroup>
    <Compile Remove="Addressbook.cs" />
  </ItemGroup>
  <ItemGroup>
    <None Include="Addressbook.cs" Condition=" '$(EnableDefaultCompileItems)' == 'true' " />
    <Compile Remove="ClientPacketManager.cs" />
    <None Include="ClientPacketManager.cs" />
    <Compile Remove="ServerPacketManager.cs" />
    <None Include="ServerPacketManager.cs" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Google.Protobuf" Version="3.12.3" />
  </ItemGroup>
</Project>
